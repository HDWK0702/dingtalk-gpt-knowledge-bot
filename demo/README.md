# 脱敏演示资料

这里的内容是虚构示例，只用于公开作品集和本地演示，不代表任何真实企业制度。

将 `.env` 中的 `OBSIDIAN_VAULT_PATH` 指向 `demo/knowledge` 所在目录的上一级，并将 `OBSIDIAN_KNOWLEDGE_SUBDIR` 设置为 `demo/knowledge`（或直接把这些文件复制到自己的演示 Vault）。

建议演示问题：

- `员工入职第一天需要做什么？`
- `产品支持哪些服务？`
- `系统没有资料时会怎么回答？`

演示时重点展示：业务域隔离、回答来源、无依据拒答和上下文追问。
